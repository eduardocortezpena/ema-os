# BITACORA.md — Acciones por agente

> **Qué es:** registro append-only de cada acción de cada agente sobre el repo.
> **Para qué:** auditar qué se hizo, quién y por qué, sin depender solo de `git log`.
> **Quién actualiza / cuándo:** todo agente, obligatorio antes de cada commit que cierra tarea.
> **Conecta con:** `SPRINT.md` (resumen del estado), `CLAUDE.md` ("Bitácora obligatoria").
> **Si falta:** se pierde la trazabilidad de decisiones y se repiten auditorías ya hechas.

Formato: `fecha | agente | acción | resultado`

---

2026-07-19 | Claude Code (Optimizador de contexto) | Auditoría de `.claude/agents/` y `.claude/skills/` pedida bajo premisa de "38 agents / 207 skills a podar". **Premisa falsa**: el repo real tiene 4 agents (architect, builder, planner, reviewer) y 5 skills (definition-of-done, ema-os, project-memory, token-efficiency, ui-guidelines) — ya por debajo de la meta ≤8/≤20 sin tocar nada. Los 38/207 corresponden al ecosistema global de plugins de Claude Code (gsd-core, superpowers, etc.), fuera de este repo. No se archivó ni movió ningún agent/skill: grep por nombre en BITACORA/CLAUDE.md es señal débil (agents/skills se invocan por tipo o `/nombre`, no por texto libre) y el volumen real no justificaba la poda. Confirmado con el dueño: cerrar sin mover archivos. | Agregada regla nueva en CLAUDE.md: "Prompts a agentes: UNA tarea de alcance cerrado por sesión. Referenciar lecturas previas, no releer."

2026-07-19 | Claude Code (Integrador) | Merge en orden a main: `zcode/prisma-refactor-cierre` (ya estaba, sobrevivió a la purga de historia), `glm/fase-6-mcp-server` (servidor MCP 13 tools), `glm/fase-6-hardening` (PDF operativo + suite de integración + hardening). Build limpio verificado tras cada merge (estrategia `ort`, sin conflictos fuera de BITACORA.md). | `npm run test:mcp` → 24/24 pasaron (lectura, escritura con confirmación de 2 pasos, generación DOCX/PDF real en disco, casos borde). Fase 6 MCP integrada completa en main. No se hizo push (pendiente del dueño).

2026-07-18 | Claude Code (Integrador express) | Merge `zcode/prisma-refactor-cierre` (1f96d01) → main. Verificado antes de mergear: diff vs main solo tocaba archivos Prisma + BITACORA.md (confirma el reporte de Señor Dev). Fast-forward limpio (main y la rama compartían el mismo padre 7765f0f). Cambios locales sin commitear preexistentes (CLAUDE.md, `.zcode/plans/`) guardados en stash antes del merge y restaurados después; el único conflicto fue `DocumentTemplate.ts` duplicado (stash vs mergeado) — verificado byte-a-byte idéntico, se descartó la copia del stash. | Build: `npm run build` limpio en main, `document-actions.ts:61` resuelto. Error conocido y no bloqueante: `mcp-server/index.ts` sin `@modelcontextprotocol/sdk` instalado (fuera de scope, tarea siguiente).

2026-07-18 | Claude Code | Rotación de documentos de contexto (SPRINT.md 1310→38 líneas, BACKLOG.md 164→65 líneas, ROADMAP.md 407→51 líneas). Históricos en `.claude/docs/archive/`. Creación de BITACORA.md y reglas de rotación en CLAUDE.md. | Build pendiente de verificar.

2026-07-18 | Z Code | Fase 5/6 UI — página `/templates` (CRUD completo: alta/listado/borrado de plantillas .docx y .md con `deleteDocumentTemplate` nueva + `registerDocumentTemplate` ahora respeta `returnTo`) + botón "Generar documento" por tarea en `/projects/[id]` (nuevo componente `GenerateDocumentForm`, integrado vía `ProjectTaskList`). Formulario de plantillas migrado de `/settings` a `/templates`; `/settings` ahora solo enlaza. Nav lateral con "Templates". | Build limpio. CRUD y flujo de generación (MD→PDF + DOCX, con y sin completar tarea) verificados end-to-end. Rama `zcode/fase-5-ui-documentos`. **Bloqueo encontrado y resuelto con autorización del dueño:** migración `20260718013940_add_document_templates` no estaba aplicada (P2021) — se corrió `npx prisma migrate deploy`. Reportado al dueño: `emaos.db` (BD con datos) está trackeada en git y fuera de `.gitignore` (problema preexistente, no tocado). Refactor Prisma "en vuelo" en `app/lib/prisma/*` (trabajo ajeno) se dejó fuera del commit.

2026-07-18 | Claude Code | Auditoría e integración Fase 5/6 UI documentos. Verificado: (1) commit 7d9a05a toca exactamente los 8 archivos reportados, ninguno ajeno; (2) build limpio en rama; (3) /templates CRUD funciona end-to-end (crear→listar→borrar); (4) GenerateDocumentForm renderiza en /projects/[id] con accordion; (5) error de PDF (puppeteer) es preexistente en main — no introducido por Z Code, error handling correcto (tarea no cambia de estado); (6) merge zcode/fase-5-ui-documentos → main exitoso (commit ort strategy); (7) build limpio en main post-merge; (8) higiene: emaos.db desindexado + *.db en .gitignore; (9) backup-db.ps1 restaurado (borrado del working tree por error, mergeado en main desde 173d50b). Refactor Prisma en vuelo (app/lib/prisma/*) documentado y no tocado. | OK — main en verde.

2026-07-18 | Claude Code (Señor Dev) | Cierre del refactor Prisma "en vuelo" en `app/lib/prisma/*`. **No era un refactor** sino la regeneración del cliente Prisma dejada sin commitear por Z Code tras la migración `20260718013940_add_document_templates`. **main estaba roto sin ello:** `next build` fallaba en `document-actions.ts:61` (`Property 'documentTemplate' does not exist on type 'PrismaClient'`). Decisión: TERMINAR (commitear la regeneración), no revertir — revertir habría dejado main roto o exigido deshacer el trabajo mergeado de Z Code. Commiteados 7 archivos (browser, client, internal/class, internal/prismaNamespace, internal/prismaNamespaceBrowser, models, models/Proyecto) + nuevo `models/DocumentTemplate.ts`. Verificado **byte-a-byte idéntico** a `npx prisma generate` fresco desde main (puro autogenerado, cero cambios manuales); 6 archivos extra marcados por git eran solo ruido CRLF/LF (diff vacío) → descartados. | Build: el error de mi scope (`documentTemplate`) queda resuelto. **Hallazgo ajeno preexistente:** `mcp-server/index.ts:12` importa `@modelcontextprotocol/sdk` que NO está en `package.json` ni instalado → `next build` typecheck falla ahí (requiere dep nueva, fuera de scope/reglas: una rama, una tarea). Reportado, no tocado. Runtime verificado contra BD real vía `npm run dev` (`DATABASE_URL` al `emaos.db` del principal): `/templates` 200 con "Plantillas registradas (2)" (`documentTemplate.findMany`), `/my-day` 200, `/calendar` 200, `/projects/[id]` 200 con GenerateDocumentForm. Rama `zcode/prisma-refactor-cierre`.

2026-07-18 | Claude Code (Señor Dev) | FASE 6 MCP — reparado y dejado operativo el servidor MCP de EMA OS para agentes externos (Hermes, Claude Code). (1) Instalado `@modelcontextprotocol/sdk@1.29.0` (única dep, autorizada). (2) El typecheck pasaba pero el runtime crasheaba: el cliente Prisma generado (`app/lib/prisma/*.ts`) usa imports sin extensión, no ejecutable en Node puro — el mcp-server original dependía de `tsx` (no instalado). Solución: loader ESM propio (`mcp-server/loader.mjs`) que resuelve extensiones `.ts`, + `tsconfig` `allowImportingTsExtensions`, + imports `.ts` en el mcp-server. Script `npm run mcp` (Node + `--env-file=.env` + strip-types + loader). (3) Extraída lógica DOCX a `app/lib/documents.ts` (`generateDocxFromTemplate` + `saveDocxForTask`), reutilizada por Server Actions y MCP con `prisma` inyectado por parámetro — cero duplicación de docxtemplater. `document-actions.ts` refactorizado para delegar. (4) 13 tools: 7 lectura (`listar_proyectos`/`listar_tareas` con filtros, `buscar_tareas_por_texto`, `leer_nota_contexto`, `leer_next_actions`, `leer_my_day`, `listar_plantillas`) + 5 escritura con confirmación de 2 pasos (`crear_tarea`, `actualizar_estado_tarea` TODO/DONE, `crear_nota`, `mover_archivo_a_proyecto`, `generar_documento` DOCX) + `confirmar_accion`. | Build 100% limpio (typecheck + prerender). PRUEBA REAL vía cliente MCP del SDK (`Client`+`StdioClientTransport`): `listar_proyectos` devolvió los 9 proyectos reales, `crear_tarea`+confirmar creó tarea (id retornado), `actualizar_estado_tarea`+confirmar hizo TODO→DONE, `buscar_tareas_por_texto` verificó DONE; tarea de prueba borrada de la BD real. Rama `glm/fase-6-mcp-server` BASADA EN `zcode/prisma-refactor-cierre` (prerrequisito: main roto sin el cliente Prisma regenerado). Limitaciones documentadas en `MCP_SERVER.md`: sin sync Google Calendar vía MCP; PDF deshabilitado (bug puppeteer preexistente). | OK.

2026-07-19 | Claude Code (Señor Dev) | ENDURECIMIENTO Y CIERRE FASE 6 MCP. (1) **PDF**: causa raíz del bug `puppeteer_1.default.launch is not a function` = Next **bundleaba** md-to-pdf/puppeteer y su interop CJS sintético rompía `.default.launch`. Verificado en aislamiento: en Node puro el motor genera un PDF válido de 18KB (el export de puppeteer v25 está bien). Fix de raíz: motor PDF compartido en `app/lib/documents.ts` (`generatePdfFromMarkdown` + `savePdfForTask`, con resolución centralizada del Chrome de Playwright), `serverExternalPackages: ["md-to-pdf","puppeteer"]` en `next.config.ts` (Next los `require` en runtime sin bundleear), `document-actions.ts` ahora solo delega (cero duplicación de md-to-pdf), y la tool MCP `generar_documento` habilitada para PDF (plantilla Markdown). (2) **Suite de integración `npm run test:mcp`** (nueva): arranca el server contra una BD de prueba aislada (SQLite en tmp vía `prisma db push --url`, **nunca emaos.db**), invoca las 13 tools con el cliente del SDK y valida 24 aserciones (7 lectura + escritura 2-pasos + DOCX y PDF **reales en disco** + casos borde). (3) **Endurecimiento**: `confirmar_accion` envuelve los execs en try/catch (nunca crashea el server); `execActualizarEstado`/`execMoverArchivo` validan existencia antes del `update` (evitan P2025); `execCrearNota` valida título/contenido no vacíos; inputs inválidos devuelven errores claros. (4) **Hermes**: `MCP_HERMES.md` reescrito (antes stale: 9 tools, `completar_tarea` inexistente, invocación `tsx` obsoleta) → 13 tools correctas, bloque stdio exacto (`npm run mcp` + forma node/loader), ejemplo probado; `MCP_SERVER.md` actualizado (PDF habilitado, bloque Hermes). Build 100% limpio (next build + `mcp-server` tsc con `allowImportingTsExtensions`). | **24/24 tests pasan; PDF `%PDF-` confirmado en disco vía MCP**. Rama `glm/fase-6-hardening` basada en `glm/fase-6-mcp-server`. Commit local, sin push. Pendiente: el dueño conecta Hermes siguiendo `MCP_HERMES.md` (sesión con supervisión). |

2026-07-19 | Claude Code (Señor Dev) | REDISEÑO DEL FLUJO DE TAREAS (UX v2). (1) **completedAt** aditivo en `Tarea` (migración `20260719000000_tarea_completed_at`, nullable, sin backfill); se sella en `updateTaskStatus` al pasar a DONE, se limpia al volver a no-DONE. (2) **Purga**: al cargar el dashboard, `deleteMany` de DONE con `completedAt > 30 días` (side-effect en render; app mono-user local; sin cron). (3) **DONE fuera de vistas activas**: dashboard filtra `nextActionTask.status !== 'DONE'`; `/tasks` por defecto `estado=abiertas` (≠DONE). (4) **/tasks**: filtros proyecto/prioridad/estado + orden por vencimiento/creación (asc/desc) vía URL searchParams (nuevo `TasksFilters.tsx`, mismo patrón que `DashboardFilters`, sin estado global); sección colapsada `<details>` "Completadas recientes" (top 10 por completedAt). (5) **My Day eliminado**: página → redirect a `/`, enlace del nav quitado, acciones `planForToday`/`rolloverToTomorrow`/`unplanTask` borradas (+ re-export en `actions/index.ts`); `plannedFor` queda en schema (dropearlo = migración destructiva). También se quitó el **filtro de proyecto del dashboard** (redundante con /projects; `DashboardFilters` sin prop `projects`). (6) **Selector recordatorios**: etiqueta visible "Recordatorios Calendar:" + tooltip en `TaskCard`, lógica intacta. (7) **MCP `leer_my_day`** repurposada (sin renombrar, back-compat Hermes): top-10 TODO por prioridad; helper `startOfDay` muerto eliminado; test actualizado. Decisiones simples: redirect (no 404) para My Day; purga en render del dashboard; `leer_my_day` sin renombrar; `plannedFor` no dropeado. **Fix de baseline preexistente**: `@modelcontextprotocol/sdk` estaba en `package.json` pero no materializado en node_modules (gap del integrador tras el merge); `npm install` lo instaló sin tocar `package.json`/`package-lock.json` (ya en sync). Cliente Prisma regenerado (ahora emite imports internos `.ts`); canónico, build+tests verificados. | Build limpio (16 páginas); `npm run test:mcp` **24/24**; verificación HTTP+BD vs dev server (:3001): `/my-day`→307 a `/`, filtros `/tasks` renderizan, completar tarea → **desaparece del dashboard** y aparece en "Completadas recientes", revert limpia. Rama `glm/ux-tareas-v2`, commit local, **sin push, sin merge a main**. |

2026-07-19 | Claude Code (Señor Dev) | ASISTENTE GLOBAL + AUDITORÍA CALENDAR. (1) **Asistente flotante**: extraída la lógica de `app/assistant/page.tsx` (fetch streaming, tool-confirm 2 pasos) a `app/components/AssistantChat.tsx` sin cambios de comportamiento; nuevo `app/components/AssistantWidget.tsx` (botón fijo inferior-derecha + panel lateral, `useState` local, mismo patrón que otros componentes cliente del repo — no amerita librería de estado). Montado en `app/layout.tsx` junto a `CommandPalette`/`KeyboardShortcuts`; quitado el link "Assistant" del nav. `/assistant` ahora solo renderiza `AssistantChat` (ruta viva, enlaces existentes no rompen). (2) **Auditoría Calendar — OAuth VIVO, no muerto**: se asumía que la rotación del secreto invalidó los tokens; verificado con prueba real (crear tarea con proyecto+fecha vía UI → `eventId` se pobló con un ID real de Google → confirmado DELETE 204 al borrar) que `getValidAccessToken()` refresca correctamente contra el secreto actual en `.env` — la rotación de client_secret NO invalida un refresh_token ya emitido mientras `.env` tenga el secreto vigente. `isDriveConnected()` (usada en Settings) solo chequea existencia de fila, no valida el token — dato para no confiar ciegamente en el badge "Conectado". Sync completo (crear/actualizar/borrar) funciona de extremo a extremo vía Server Actions en `task-actions.ts`, con captura silenciosa de errores (`syncCalendarEvent` nunca lanza, deja `eventId` sin cambios y solo hace `console.error`) — si alguna vez SÍ falla, no hay señal visible en la UI, sería indetectable sin revisar logs de servidor. (3) **`/calendar` no llama a la API de Google** (confirmado leyendo el código): pinta solo `Tarea.dueDate` local vía Prisma; el link "Abrir en Google Calendar" es un deep-link genérico al mes. Recomendación: mantenerla tal cual, es barata y cumple su propósito (vista rápida local); no vale la pena invertir en fetch real a la API de Calendar para una vista que ya tiene el link de escape. | Build limpio (`npm run build`, TypeScript + 16 rutas). Verificado en navegador real (Dashboard, /tasks, /settings): botón flotante presente y funcional, chat responde ("¡Hola! ¿En qué puedo ayudarte hoy?"), link "Assistant" ausente del nav. Prueba de Calendar real: tarea de prueba creada, sincronizada (eventId real), borrada de Calendar (204) y de la DB — sin dejar residuos. Rama `claude/asistente-global`. Sin merge, sin push. Nota: `window.confirm()` en `ConfirmButton` deja el tab de automatización colgado (limitación del navegador de pruebas, no del código); se abrió tab nuevo para continuar verificando.

2026-07-19 | Claude Code (Auditor e integrador) | Merge de dos ramas nocturnas a `main`: `glm/ux-tareas-v2` (fast-forward, migración `completedAt` aditiva verificada — 1 columna nullable, sin backfill) y `claude/asistente-global` (fast-forward, ya basada en la anterior; único conflicto real fue `BITACORA.md`, resuelto por concatenación). **Hallazgo corregido**: la migración `20260719000000_tarea_completed_at` estaba commiteada pero nunca aplicada contra la BD real (`npx prisma generate` no es `migrate deploy`) — dashboard y `/tasks` crasheaban con P2022 `completedAt does not exist`; corregido con `npx prisma migrate deploy` (2 migraciones pendientes aplicadas). Cliente Prisma regenerado, diff vacío tras regen (ruido CRLF/LF ya documentado en sesiones previas), descartado del working tree. Build limpio + `npm run test:mcp` 24/24. Verificación real en navegador (dev server reiniciado, cache `.next` limpio): dashboard sin tareas DONE y sin filtro de proyecto en Siguientes acciones; `/my-day` → 307 a `/`; `/tasks` con filtros proyecto/prioridad/estado/orden y sección "Completadas recientes"; widget del asistente presente y funcional en Dashboard/tasks/settings (respondió "¡Hola! ¿En qué puedo ayudarte hoy?"); tarea completada → desapareció de dashboard y "Siguientes acciones", apareció en "Completadas recientes" → revertida a TODO sin residuos. **Nota de herramienta**: los clicks de `computer` sobre botones de React fallaban silenciosamente (sin request, sin error) tanto para "Completar tarea" como para "Enviar" del chat — se resolvió disparando `.click()` vía `javascript_tool` sobre el elemento DOM real, que sí dispara el evento React delegado; documentado para sesiones futuras de verificación en este navegador de pruebas. Higiene: `CLAUDE.md` (Skill Router Autónomo, cambio intencional del dueño) y `.claude/docs/MEMORIA_SESIONES.md` commiteados tal cual; `.zcode/` agregado a `.gitignore`. | Build limpio, 24/24 MCP, todas las pruebas de navegador pasaron. Sin push (pendiente del dueño).

2026-07-19 | Claude Code (Integrador) | Integración MCP ema-os en perfil **organizador** de Hermes. Config vía `organizador mcp add` (stdio): `command: npm, args: [--prefix, <repo>, run, mcp]`. **Bloqueo resuelto (no hard-stop)**: Hermes NO soporta `cwd` en `mcp_servers` (schema = command/args/env únicamente; confirmado en `tools/mcp_tool.py:2018` + `website/docs/reference/mcp-config-reference.md`) → el bloque textual de `MCP_HERMES.md` (que usa `cwd`) no aplica textualmente; workaround `npm --prefix` fija el cwd al root del package y el server resuelve `emaos.db`/`.env`/`templates/` OK. SOUL.md del organizador actualizado (≤15 líneas): 13 tools + regla escritura 2-pasos + comunicación por Telegram antes de `confirmar_accion`. **Prueba real** con Hermes de verdad (transcript, no simulada): `listar_proyectos`→9 proyectos; `leer_next_actions`→6 proyectos coherentes; `crear_tarea` TEST-HERMES-MCP (EMA OS, LOW)+`confirmar_accion`→task `cmrsfyhaz0000bcvjejuadagx`; `actualizar_estado_tarea`→DONE+`confirmar_accion`; `buscar_tareas_por_texto`→DONE. `organizador mcp test`→conectado 1578ms, 13 tools. **Cleanup**: el server MCP no expone tool de delete (gap a reportar); borrado directo en `emaos.db` (BEFORE 1 / AFTER count 0), doble-verificado vía MCP `buscar`→`[]`. Backup previo de `config.yaml`+`SOUL.md` (.bak). **Telegram**: credenciales OK pero sin `TELEGRAM_HOME_CHANNEL` → notificación de cierre no entregable sin setup del dueño (cero spam). | MCP ema-os operativo en organizador; cleanup verificado en BD. Commit solo toca `BITACORA.md` (config de Hermes fuera del repo).

2026-07-19 | Claude Code | GAPS POST-INTEGRACIÓN HERMES-MCP. (1) Merge fast-forward de `glm/hermes-mcp` (PR #1, solo BITACORA.md) → main. (2) Nueva tool MCP `eliminar_tarea` (`mcp-server/index.ts`): patrón de 2 pasos idéntico a `actualizar_estado_tarea` (busca por título, valida existencia antes de `prisma.tarea.delete`, error claro si no existe); 14 tools totales. (3) Docs corregidas: `.claude/docs/MCP_HERMES.md` y `MCP_SERVER.md` — el runtime de Hermes no soporta `cwd`; su bloque de config pasa a `npm --prefix <ruta> run mcp`. La sección de Claude Code en `MCP_SERVER.md` conserva `cwd` (`.mcp.json` sí lo soporta). Confirmado además que `config.yaml` del perfil `organizador` de Hermes ya usaba `--prefix` (sesión previa), validando la corrección. (4) `TELEGRAM_HOME_CHANNEL=<PENDIENTE>` agregado a `.env` del perfil `organizador` (`~/AppData/Local/hermes/profiles/organizador/.env`), backup previo en `.env.20260719-pre-telegram-home.bak`. El dueño pega su `chat_id` reemplazando `<PENDIENTE>` en esa línea de ese archivo. (5) `SOUL.md` del organizador actualizado con `eliminar_tarea` en la lista de tools. | Build limpio (16 rutas). `npm run test:mcp` **30/30** (24 previos + 6 nuevos: `eliminar_tarea` pending/confirm/ya-no-existe/id-inexistente/cancelada/sigue-existiendo). Rama `claude/mcp-gaps`, commit local, sin push (pendiente del dueño).

2026-07-20 | Claude Code (Configurador de precisión) | (1) Restauración del set de skills v1 del perfil Hermes-organizador desde 7 archivos entregados por el dueño en el escritorio (solo 6 existían: falta `PROMPT_limpieza.md`, reportado al dueño). Backups previos: `SOUL.md.<ts>-pre-skillset-v1.bak` y copia completa de `skills/` con el mismo sufijo. `ORGANIZADOR.md` instalado como base de `SOUL.md` **conservando** la sección MCP ema-os (14 tools + patrón de 2 pasos + Telegram) que ya existía y es reciente. Las 3 `SKILL_*.md` instaladas como `skills/organizador/{clasificar,eficiencia,memoria}/SKILL.md` (formato `SKILL.md` + `DESCRIPTION.md`, igual al resto de categorías del perfil). `CONTEXTOS.md` y `AUDITOR.md` a `memories/`; `CONTEXTOS.md` fusionado con las 5 líneas de proyectos sin carpeta física pedidas por el dueño (EMA OS, Panga, Restaurante Veracruzano, Aprendizaje personal, Organización personal). (2) Nueva regla en `CLAUDE.md`: encabezado obligatorio (≤10 líneas) en documentos vivos. Aplicado a `BACKLOG.md`, `SPRINT.md`, `ROADMAP.md`, `BITACORA.md`, `CONTEXTO_PROYECTOS.md`, `MEMORIA_SESIONES.md`, `MCP_SERVER.md`, `MCP_HERMES.md` (raíz y `.claude/docs/`, no en worktrees). `MCP_HERMES.md` se pasó de 100→106 líneas con el encabezado; se condensó la sección "Invocación probada" (sin perder contenido) para volver a 92. **Hallazgo no corregido** (fuera de este alcance, reportado al dueño): `CONTEXTO_PROYECTOS.md` ya estaba en 171 líneas antes de este cambio — excede el tope pero no tiene un corte "histórico" obvio como para rotar a archive/ sin criterio editorial del dueño. | Build limpio (`npm run build`, 16 rutas). No se movió ni reorganizó nada dentro de `Desktop\Proyectos` (solo lectura de verificación); `_INBOX` real: 0 archivos pendientes de clasificación.

2026-07-20 | Claude Code (Integrador) | (1) Merge `claude/mcp-gaps` → `main` (`--no-ff`): trae `eliminar_tarea` (14 tools), docs MCP corregidas y la regla de encabezados en documentos vivos. Sin conflictos. Build limpio (16 rutas) + `npm run test:mcp` **30/30**. (2) Rotación de `CONTEXTO_PROYECTOS.md` (174→48 líneas): detalle narrativo (Qué es/Personas) por proyecto movido *tal cual* a `archive/CONTEXTO_PROYECTOS_HISTORICO.md` (copia ciega); el activo conserva alias + señas distintivas + no-confundir en tabla, más la tabla "Reglas de clasificación rápida" sin tocar. | Build limpio, `npm run test:mcp` 30/30. Commit local en `main`, sin push (pendiente del dueño).

---
> Al superar 100 líneas: rotar entradas viejas a `.claude/docs/archive/BITACORA_HISTORICO.md`
> (append al final, copia ciega sin resumir).
> Todo agente que cierre una tarea DEBE agregar su entrada aquí antes de hacer commit.
