# BITACORA.md — Acciones por agente

Formato: `fecha | agente | acción | resultado`

---

2026-07-18 | Claude Code (Integrador express) | Merge `zcode/prisma-refactor-cierre` (1f96d01) → main. Verificado antes de mergear: diff vs main solo tocaba archivos Prisma + BITACORA.md (confirma el reporte de Señor Dev). Fast-forward limpio (main y la rama compartían el mismo padre 7765f0f). Cambios locales sin commitear preexistentes (CLAUDE.md, `.zcode/plans/`) guardados en stash antes del merge y restaurados después; el único conflicto fue `DocumentTemplate.ts` duplicado (stash vs mergeado) — verificado byte-a-byte idéntico, se descartó la copia del stash. | Build: `npm run build` limpio en main, `document-actions.ts:61` resuelto. Error conocido y no bloqueante: `mcp-server/index.ts` sin `@modelcontextprotocol/sdk` instalado (fuera de scope, tarea siguiente).

2026-07-18 | Claude Code | Rotación de documentos de contexto (SPRINT.md 1310→38 líneas, BACKLOG.md 164→65 líneas, ROADMAP.md 407→51 líneas). Históricos en `.claude/docs/archive/`. Creación de BITACORA.md y reglas de rotación en CLAUDE.md. | Build pendiente de verificar.

2026-07-18 | Z Code | Fase 5/6 UI — página `/templates` (CRUD completo: alta/listado/borrado de plantillas .docx y .md con `deleteDocumentTemplate` nueva + `registerDocumentTemplate` ahora respeta `returnTo`) + botón "Generar documento" por tarea en `/projects/[id]` (nuevo componente `GenerateDocumentForm`, integrado vía `ProjectTaskList`). Formulario de plantillas migrado de `/settings` a `/templates`; `/settings` ahora solo enlaza. Nav lateral con "Templates". | Build limpio. CRUD y flujo de generación (MD→PDF + DOCX, con y sin completar tarea) verificados end-to-end. Rama `zcode/fase-5-ui-documentos`. **Bloqueo encontrado y resuelto con autorización del dueño:** migración `20260718013940_add_document_templates` no estaba aplicada (P2021) — se corrió `npx prisma migrate deploy`. Reportado al dueño: `emaos.db` (BD con datos) está trackeada en git y fuera de `.gitignore` (problema preexistente, no tocado). Refactor Prisma "en vuelo" en `app/lib/prisma/*` (trabajo ajeno) se dejó fuera del commit.

2026-07-18 | Claude Code | Auditoría e integración Fase 5/6 UI documentos. Verificado: (1) commit 7d9a05a toca exactamente los 8 archivos reportados, ninguno ajeno; (2) build limpio en rama; (3) /templates CRUD funciona end-to-end (crear→listar→borrar); (4) GenerateDocumentForm renderiza en /projects/[id] con accordion; (5) error de PDF (puppeteer) es preexistente en main — no introducido por Z Code, error handling correcto (tarea no cambia de estado); (6) merge zcode/fase-5-ui-documentos → main exitoso (commit ort strategy); (7) build limpio en main post-merge; (8) higiene: emaos.db desindexado + *.db en .gitignore; (9) backup-db.ps1 restaurado (borrado del working tree por error, mergeado en main desde 173d50b). Refactor Prisma en vuelo (app/lib/prisma/*) documentado y no tocado. | OK — main en verde.

2026-07-18 | Claude Code (Señor Dev) | Cierre del refactor Prisma "en vuelo" en `app/lib/prisma/*`. **No era un refactor** sino la regeneración del cliente Prisma dejada sin commitear por Z Code tras la migración `20260718013940_add_document_templates`. **main estaba roto sin ello:** `next build` fallaba en `document-actions.ts:61` (`Property 'documentTemplate' does not exist on type 'PrismaClient'`). Decisión: TERMINAR (commitear la regeneración), no revertir — revertir habría dejado main roto o exigido deshacer el trabajo mergeado de Z Code. Commiteados 7 archivos (browser, client, internal/class, internal/prismaNamespace, internal/prismaNamespaceBrowser, models, models/Proyecto) + nuevo `models/DocumentTemplate.ts`. Verificado **byte-a-byte idéntico** a `npx prisma generate` fresco desde main (puro autogenerado, cero cambios manuales); 6 archivos extra marcados por git eran solo ruido CRLF/LF (diff vacío) → descartados. | Build: el error de mi scope (`documentTemplate`) queda resuelto. **Hallazgo ajeno preexistente:** `mcp-server/index.ts:12` importa `@modelcontextprotocol/sdk` que NO está en `package.json` ni instalado → `next build` typecheck falla ahí (requiere dep nueva, fuera de scope/reglas: una rama, una tarea). Reportado, no tocado. Runtime verificado contra BD real vía `npm run dev` (`DATABASE_URL` al `emaos.db` del principal): `/templates` 200 con "Plantillas registradas (2)" (`documentTemplate.findMany`), `/my-day` 200, `/calendar` 200, `/projects/[id]` 200 con GenerateDocumentForm. Rama `zcode/prisma-refactor-cierre`.

2026-07-18 | Claude Code (Señor Dev) | FASE 6 MCP — reparado y dejado operativo el servidor MCP de EMA OS para agentes externos (Hermes, Claude Code). (1) Instalado `@modelcontextprotocol/sdk@1.29.0` (única dep, autorizada). (2) El typecheck pasaba pero el runtime crasheaba: el cliente Prisma generado (`app/lib/prisma/*.ts`) usa imports sin extensión, no ejecutable en Node puro — el mcp-server original dependía de `tsx` (no instalado). Solución: loader ESM propio (`mcp-server/loader.mjs`) que resuelve extensiones `.ts`, + `tsconfig` `allowImportingTsExtensions`, + imports `.ts` en el mcp-server. Script `npm run mcp` (Node + `--env-file=.env` + strip-types + loader). (3) Extraída lógica DOCX a `app/lib/documents.ts` (`generateDocxFromTemplate` + `saveDocxForTask`), reutilizada por Server Actions y MCP con `prisma` inyectado por parámetro — cero duplicación de docxtemplater. `document-actions.ts` refactorizado para delegar. (4) 13 tools: 7 lectura (`listar_proyectos`/`listar_tareas` con filtros, `buscar_tareas_por_texto`, `leer_nota_contexto`, `leer_next_actions`, `leer_my_day`, `listar_plantillas`) + 5 escritura con confirmación de 2 pasos (`crear_tarea`, `actualizar_estado_tarea` TODO/DONE, `crear_nota`, `mover_archivo_a_proyecto`, `generar_documento` DOCX) + `confirmar_accion`. | Build 100% limpio (typecheck + prerender). PRUEBA REAL vía cliente MCP del SDK (`Client`+`StdioClientTransport`): `listar_proyectos` devolvió los 9 proyectos reales, `crear_tarea`+confirmar creó tarea (id retornado), `actualizar_estado_tarea`+confirmar hizo TODO→DONE, `buscar_tareas_por_texto` verificó DONE; tarea de prueba borrada de la BD real. Rama `glm/fase-6-mcp-server` BASADA EN `zcode/prisma-refactor-cierre` (prerrequisito: main roto sin el cliente Prisma regenerado). Limitaciones documentadas en `MCP_SERVER.md`: sin sync Google Calendar vía MCP; PDF deshabilitado (bug puppeteer preexistente). | OK.

2026-07-19 | Claude Code (Señor Dev) | ENDURECIMIENTO Y CIERRE FASE 6 MCP. (1) **PDF**: causa raíz del bug `puppeteer_1.default.launch is not a function` = Next **bundleaba** md-to-pdf/puppeteer y su interop CJS sintético rompía `.default.launch`. Verificado en aislamiento: en Node puro el motor genera un PDF válido de 18KB (el export de puppeteer v25 está bien). Fix de raíz: motor PDF compartido en `app/lib/documents.ts` (`generatePdfFromMarkdown` + `savePdfForTask`, con resolución centralizada del Chrome de Playwright), `serverExternalPackages: ["md-to-pdf","puppeteer"]` en `next.config.ts` (Next los `require` en runtime sin bundleear), `document-actions.ts` ahora solo delega (cero duplicación de md-to-pdf), y la tool MCP `generar_documento` habilitada para PDF (plantilla Markdown). (2) **Suite de integración `npm run test:mcp`** (nueva): arranca el server contra una BD de prueba aislada (SQLite en tmp vía `prisma db push --url`, **nunca emaos.db**), invoca las 13 tools con el cliente del SDK y valida 24 aserciones (7 lectura + escritura 2-pasos + DOCX y PDF **reales en disco** + casos borde). (3) **Endurecimiento**: `confirmar_accion` envuelve los execs en try/catch (nunca crashea el server); `execActualizarEstado`/`execMoverArchivo` validan existencia antes del `update` (evitan P2025); `execCrearNota` valida título/contenido no vacíos; inputs inválidos devuelven errores claros. (4) **Hermes**: `MCP_HERMES.md` reescrito (antes stale: 9 tools, `completar_tarea` inexistente, invocación `tsx` obsoleta) → 13 tools correctas, bloque stdio exacto (`npm run mcp` + forma node/loader), ejemplo probado; `MCP_SERVER.md` actualizado (PDF habilitado, bloque Hermes). Build 100% limpio (next build + `mcp-server` tsc con `allowImportingTsExtensions`). | **24/24 tests pasan; PDF `%PDF-` confirmado en disco vía MCP**. Rama `glm/fase-6-hardening` basada en `glm/fase-6-mcp-server`. Commit local, sin push. Pendiente: el dueño conecta Hermes siguiendo `MCP_HERMES.md` (sesión con supervisión). |

---
> Al superar 100 líneas: rotar entradas viejas a `.claude/docs/archive/BITACORA_HISTORICO.md`
> (append al final, copia ciega sin resumir).
> Todo agente que cierre una tarea DEBE agregar su entrada aquí antes de hacer commit.
